[package]
name = "aster-platform"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "平台抽象层 — 封装 winit/wgpu/rodio 等平台后端，提供窗口、输入、音频设备抽象"

[dependencies]
# aster-platform 仅依赖标准库，不引入任何外部依赖
# 这是架构分层的最底层（Architecture.md §4.1）
